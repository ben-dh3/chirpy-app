import { Nunito_Sans, Sen } from 'next/font/google';
export const nunito_sans = Nunito_Sans({ 
    weight: ['400', '700'],
    subsets: ['latin']
});
export const sen = Sen({ 
    weight: ['500'],
    subsets: ['latin']
});